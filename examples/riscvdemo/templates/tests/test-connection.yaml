apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "riscvdemo.fullname" . }}-test-connection"
  labels:
    {{- include "riscvdemo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "riscvdemo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
